<div class="lists_banner">
  <h1>All movie's lists</h1>
</div>
<div class="my_lists">
  <h2>My lists</h2>
  <%= link_to "Create a new list", new_list_path %>
</div>
<% @lists.each do |list| %>
  <p><%= link_to list.name, list_path(list) %></p>
<% end %>
